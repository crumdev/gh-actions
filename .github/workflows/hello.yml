name: first

env:
  user: nathan

on: 
  push:
    branches:
      - main
      - ch02
  pull_request:
    branches:
      - main

jobs:
  
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello $user"
    
